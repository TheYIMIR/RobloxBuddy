<Page x:Class="RobloxBuddy.Pages.GamesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RobloxBuddy.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Games">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Games" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtSearch" Grid.Column="0" Margin="0,0,10,0" Padding="5" PlaceholderText="Search games..."/>
            <Button x:Name="btnSearch" Grid.Column="1" Content="Search" Style="{StaticResource RobloxButton}"/>
        </Grid>

        <TabControl Grid.Row="2">
            <TabItem Header="Popular">
                <ListView x:Name="lvPopularGames" SelectionChanged="lvGames_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding ThumbnailUrl}" Width="70" Height="70" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding CreatorName}" Foreground="Gray" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding ActivePlayers}" FontSize="12" Foreground="#02B757"/>
                                        <TextBlock Text=" active players" FontSize="12" Foreground="#02B757"/>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="2" Content="Play" Style="{StaticResource RobloxButton}" 
                                        Padding="10,3" Click="btnPlayGame_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>

            <TabItem Header="Recently Played">
                <ListView x:Name="lvRecentGames" SelectionChanged="lvGames_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding ThumbnailUrl}" Width="70" Height="70" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding CreatorName}" Foreground="Gray" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding ActivePlayers}" FontSize="12" Foreground="#02B757"/>
                                        <TextBlock Text=" active players" FontSize="12" Foreground="#02B757"/>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="2" Content="Play" Style="{StaticResource RobloxButton}" 
                                        Padding="10,3" Click="btnPlayGame_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>

            <TabItem Header="Favorites">
                <ListView x:Name="lvFavoriteGames" SelectionChanged="lvGames_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding ThumbnailUrl}" Width="70" Height="70" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding CreatorName}" Foreground="Gray" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding ActivePlayers}" FontSize="12" Foreground="#02B757"/>
                                        <TextBlock Text=" active players" FontSize="12" Foreground="#02B757"/>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="2" Content="Play" Style="{StaticResource RobloxButton}" 
                                        Padding="10,3" Click="btnPlayGame_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
</Page>